NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://ereklamo:ereklamo@localhost:5432/ereklamo
JWT_SECRET=replace-with-at-least-32-characters
REFRESH_SECRET=replace-with-another-32-character-secret
BCRYPT_SALT_ROUNDS=12
CORS_ORIGIN=http://127.0.0.1:8080,http://localhost:8080

# Optional: only needed if backend modules use the Supabase JS client directly.
SUPABASE_URL=
SUPABASE_ANON_KEY=
